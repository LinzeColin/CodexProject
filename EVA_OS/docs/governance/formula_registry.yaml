governance_spec_version: "1.0.0"
project_id: "EVA_OS"
formulas:
  - formula_id: "FORM-001"
    model_id: "MOD-001"
    assumption_ids:
      - "ASM-001"
      - "ASM-003"
    status: "active"
    expression: "Strategy rules: MA target=1 if SMA(short)>SMA(long) else 0 or -1 if long_only=false; RSI invest after RSI<entry until RSI>exit; Bollinger invest when z<=-num_std until z>=exit_z; Breakout invest after close>prior rolling_high until close<prior rolling_low; Momentum select top_n by pct_change(lookback) and equal weight; Alipay buy floor(abs(daily_return)*buy_base_amount), sell by highest return threshold; Enhanced Alipay modifies buy amount by oversold/weak/trend multipliers and max_position_weight cap."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/strategies/trend/ma_crossover.py:1"
      - "EVA_OS/src/quantlab/strategies/behavioral/alipay.py:11"
      - "EVA_OS/src/quantlab/strategies/profiles.py:322"
    test_refs:
      - "EVA_OS/tests/test_backtest.py"
      - "EVA_OS/tests/test_alipay_strategy.py"
      - "EVA_OS/tests/test_strategy_profiles.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-001"
  - formula_id: "FORM-002"
    model_id: "MOD-002"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
      - "ASM-003"
    status: "active"
    expression: "Target-weight path executes prior-bar target_weight at next open; slip_price=open*(1+side*(slippage_bps+market_impact_bps)/10000); commission=max(abs(notional)*commission_rate,min_commission); equity=cash+quantity*close; total_return=end_equity/initial_cash-1; annualized=(1+total_return)^(annualization/periods)-1; sharpe=mean_return*annualization/volatility; cost_total=sum(execution_cost)."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/backtest/engine.py:13"
      - "EVA_OS/src/quantlab/backtest/metrics.py:22"
    test_refs:
      - "EVA_OS/tests/test_backtest.py"
      - "EVA_OS/tests/test_alipay_strategy.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-001"
  - formula_id: "FORM-003"
    model_id: "MOD-003"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
    status: "active"
    expression: "score starts 0; missing data quality +2; bad data quality +3; total_return<=0 +3; max_drawdown<limit +3; cost_ratio>max_cost_ratio +2; fragile stability +2; failed train-test +3 or watch +1; failed walk-forward +6 or watch +2; missing evidence forces NeedsMoreEvidence; otherwise score>=6 DoNotUse, score>=2 WatchOnly, else ContinueResearch."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/risk/gates.py:16"
    test_refs:
      - "EVA_OS/tests/test_risk_gates.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-002"
  - formula_id: "FORM-004"
    model_id: "MOD-004"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
      - "ASM-003"
    status: "active"
    expression: "dimension scores are clamped 0..10; overall score=round(mean(dimension_scores)*10). Status: missing evidence -> NeedsMoreEvidence; risk_gate DoNotUse -> DoNotUse; score>=80 ContinueResearch; >=60 WatchOnly; >=40 NeedsMoreEvidence; else DoNotUse."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/risk/decision_quality.py:36"
    test_refs:
      - "EVA_OS/tests/test_decision_quality.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-002"
  - formula_id: "FORM-005"
    model_id: "MOD-005"
    assumption_ids:
      - "ASM-002"
      - "ASM-007"
    status: "active"
    expression: "Data quality status Pass when required datetime/open/high/low/close/volume have zero missing values and zero duplicate datetimes, else Review; checksum=sha256(sorted required columns CSV). Cross-source close_diff_pct=(max_close-min_close)/mean_close; status Pass if max_diff<=tolerance_pct, Review otherwise; fewer than two provider frames yields InsufficientData."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/data/quality.py:34"
      - "EVA_OS/src/quantlab/data/validation.py:34"
    test_refs:
      - "EVA_OS/tests/test_data_quality.py"
      - "EVA_OS/tests/test_validation_execution.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-003"
  - formula_id: "FORM-006"
    model_id: "MOD-006"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
    status: "active"
    expression: "priority_score=clamp(20+status_score+gap_score+foundational_bonus+report_gap_bonus+source_report_bonus+symbol_market_bonus-blocker_penalties+recency_score,0,100). Execution supports CrossSourceValidation only; unsupported/missing inputs/fewer than two providers return Blocked and NeedsMoreEvidence."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/research/validation_priority.py:52"
      - "EVA_OS/src/quantlab/research/validation_execution.py:37"
    test_refs:
      - "EVA_OS/tests/test_validation_priority.py"
      - "EVA_OS/tests/test_validation_execution.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-004"
  - formula_id: "FORM-007"
    model_id: "MOD-007"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
      - "ASM-007"
    status: "active"
    expression: "Each inspected artifact receives trust_status; audit_status=Blocked if rejected_count>0, Review if review_count>0, else Pass. REJECTED maps Reject, NEEDS_REVIEW/PARSED_CANDIDATE/RAW_IMPORTED map Watch, ARCHIVED maps Observe, otherwise Actionable."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/system/data_trust.py:48"
    test_refs:
      - "EVA_OS/tests/test_data_trust_audit.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
  - formula_id: "FORM-008"
    model_id: "MOD-008"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
    status: "active"
    expression: "Integration status Fail if any layer Fail, Review if any layer Review, else Pass. Daily readiness Blocked if any core gate Fail/Blocked, NeedsReview if any gate not Pass, else ReadyForResearch. Command center Blocked if daily readiness blocked or integration fail or business subsystem fail; NeedsReview for non-ready gaps; else ReadyForResearch."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/system/integration_audit.py:27"
      - "EVA_OS/src/quantlab/system/daily_readiness.py:16"
      - "EVA_OS/src/quantlab/executive/command_center.py:208"
    test_refs:
      - "EVA_OS/tests/test_integration_audit.py"
      - "EVA_OS/tests/test_daily_readiness.py"
      - "EVA_OS/tests/test_command_center.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
  - formula_id: "FORM-009"
    model_id: "MOD-009"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
    status: "active"
    expression: "Evidence score starts 0: schema +20, linked Word report +10, data quality pass/info +20 or present-but-not-pass +8, cross validation pass/info +20 or present-but-not-pass +8, risk status +15, decision quality status +15, minus min(30,3*missing_count), clamped 0..100. Readiness follows DoNotUse first, then missing evidence, NeedsMoreEvidence, WatchOnly, ContinueResearch."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/reports/decision_support.py:28"
      - "EVA_OS/src/quantlab/research/report_gap_tasks.py:22"
    test_refs:
      - "EVA_OS/tests/test_report_decision_support.py"
      - "EVA_OS/tests/test_report_gap_tasks.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-005"
  - formula_id: "FORM-010"
    model_id: "MOD-010"
    assumption_ids:
      - "ASM-001"
      - "ASM-007"
    status: "active"
    expression: "Hotspot raw score base 50 plus clipped one/five/twenty-step return, RSI, volatility penalty and drawdown terms; volatility symbols invert return terms. Stable heat applies EMA alpha=2/(span+1) with max per-slice step cap. State thresholds: >=72 strong diffusion, >=58 locally strong, >=43 neutral, >=28 locally weak, else risk cooling. Evidence gates enforce source, coverage, failure rate, sample length, slice count, cadence, freshness and concentration."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/analysis/market_hotspots.py:14"
      - "EVA_OS/src/quantlab/analysis/market_hotspots.py:831"
    test_refs:
      - "EVA_OS/tests/test_market_hotspots.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-006"
  - formula_id: "FORM-011"
    model_id: "MOD-011"
    assumption_ids:
      - "ASM-001"
      - "ASM-004"
    status: "active"
    expression: "Only Reviewed+Pass entries count. Window inflow/outflow are sums by direction; net_cashflow=inflow-outflow; daily_burn=outflow/lookback_days if outflow>0 else 0; runway_days=latest_balance/daily_burn if available. Status MissingBalance, NeedsEvidence, Critical if runway<14, Watch if runway<30, NeedsReview if net<0, StableWithPendingReview if pending, else Stable."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/business/cashflow.py:118"
    test_refs:
      - "EVA_OS/tests/test_cashflow_command.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
  - formula_id: "FORM-012"
    model_id: "MOD-012"
    assumption_ids:
      - "ASM-001"
      - "ASM-004"
    status: "active"
    expression: "risk_score=clip(impulse_score*0.45 + regret_score*0.25 + (100-necessity_score)*0.20 + (0 if planned else 10),0,100). HighImpulse if event_type Impulse or score>=70, Watch if score>=40 else Controlled. Guard MissingConsumptionEvidence if no events, NeedsEvidence if reviewed missing evidence, StopBleeding if high_risk_count>=3 or pressure>1.0, Watch if high risk or pressure>0.6, StableWithPendingReview if pending, else Stable."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/consumption/guard.py:103"
      - "EVA_OS/src/quantlab/consumption/guard.py:532"
    test_refs:
      - "EVA_OS/tests/test_consumption_guard.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-007"
  - formula_id: "FORM-013"
    model_id: "MOD-013"
    assumption_ids:
      - "ASM-001"
      - "ASM-005"
    status: "active"
    expression: "impact_score=authority*0.30+relevance*0.30+urgency*0.20+feasibility*0.20. Reviewed authoritative evidence and impact>=70 -> Actionable; impact>=50 -> Watch; missing/non-authoritative evidence -> NeedsEvidence; pending review -> PendingReview; no records -> MissingPolicyEvidence."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/policy/radar.py:109"
      - "EVA_OS/src/quantlab/policy/radar.py:351"
    test_refs:
      - "EVA_OS/tests/test_policy_radar.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
    unknown_task_ids:
      - "TASK-EVA-B-008"
  - formula_id: "FORM-014"
    model_id: "MOD-014"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
    status: "active"
    expression: "manual value benefit=revenue_generated+cost_saved+loss_avoided+asset_reuse_value; cost=ai_cost+human_time_cost; roi_score=(benefit-cost)/cost when cost>0 else null. Quantified only when review_status Reviewed and a real financial field is supplied; aggregate ROI uses reviewed totals only; no fabricated values."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/value/token_roi.py:33"
    test_refs:
      - "EVA_OS/tests/test_token_roi_ledger.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
  - formula_id: "FORM-015"
    model_id: "MOD-015"
    assumption_ids:
      - "ASM-001"
      - "ASM-003"
    status: "active"
    expression: "Code quality score=round(passed_checks/8*100); CodeReadyForReview if no findings. SmokePass when signal frame exists, has required columns and target_weight in [-1,1]. Readiness ApprovedForResearch only when profile ReadyForReview, code CodeReadyForReview, smoke SmokePass and approval Approved; otherwise ReadyForReview or NotReady."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/strategies/templates.py:136"
      - "EVA_OS/src/quantlab/strategies/templates.py:257"
    test_refs:
      - "EVA_OS/tests/test_strategy_templates.py"
      - "EVA_OS/tests/test_strategy_profiles.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
  - formula_id: "FORM-016"
    model_id: "MOD-016"
    assumption_ids:
      - "ASM-001"
      - "ASM-002"
    status: "active"
    expression: "Dev readiness checks required executable bits, zsh -n, ast.parse, status script, cache dry-run, git status and heavy-gate policy. status=Pass if fail count is 0 else Blocked. Dirty worktree is recorded but not failure; heavy release gates, market refresh, browser automation and broker/order flow are excluded by default."
    variables:
      - name: "input_payload"
        data_type: "dict/dataframe/files"
        unit: "project-specific"
        input_domain: "must satisfy implementation-specific required fields"
        description: "Runtime or local evidence input"
      - name: "score_or_status"
        data_type: "float/string"
        unit: "score/status"
        input_domain: "implementation-defined output range"
        description: "Computed model output"
    constraints:
      - "Research-only; no live orders or external account mutations"
      - "Inputs with missing evidence must fail closed or be downgraded"
      - "Heuristic constants are not treated as calibrated unless evidence exists"
    missing_policy: "Missing or invalid inputs return Review, Blocked, NeedsMoreEvidence, Missing, zero/empty output, or ValueError according to implementation."
    normalization: "See expression; score outputs are clamped where code clamps them; probabilities/ratios use implemented formulas."
    output_range: "Status enums or numeric score/risk/metric ranges documented by implementation; no executable trading output."
    boundary_conditions: "Empty frames, missing JSON, fewer providers, corrupt files, or insufficient evidence are blocked/reviewed rather than promoted."
    fallback: "Fail closed to local review task or governance unresolved task."
    code_refs:
      - "EVA_OS/src/quantlab/system/dev_readiness.py:16"
    test_refs:
      - "EVA_OS/tests/test_dev_readiness.py"
    evidence_refs:
      - "EVA_OS/docs/governance/MODEL_SPEC.md"
