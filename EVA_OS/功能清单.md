# EVA_OS 功能清单

## 当前结论

`EVA_OS` 目前不是 `governance/projects.yaml` 中登记的活跃治理项目。

当前目录只能证明存在 `EVA_OS/src/quantlab/` 目录结构和若干 Python 缓存文件；没有足够的已跟踪源码、测试、版本、变更记录或治理事实来证明可运行功能。

## 已有证据

| 项目 | 当前状态 | 证据 |
|---|---|---|
| 项目目录 | 存在 | `EVA_OS/` |
| 代码结构线索 | 存在但不足 | `EVA_OS/src/quantlab/` |
| Git 跟踪状态 | 未登记为当前活跃项目 | `governance/projects.yaml` 未包含 `EVA_OS` |
| 用户可读三基文件 | 已补为 Markdown | `功能清单.md`、`开发记录.md`、`模型参数文件.md` |

## 不能宣称的事项

- 不能宣称 `EVA_OS` 已纳入当前 Lean v2 治理。
- 不能宣称它有已验证的生产能力。
- 不能宣称现有缓存文件等同于可维护源码。
- 不能把本文件当作功能已经完成的证据。

## 下一步

如需把 `EVA_OS` 当作第 10 个活跃项目，需要单独建立迁移任务：确认项目边界、恢复或提交源码、补齐版本和变更记录、加入项目注册表、再补 canonical facts 和测试证据。
