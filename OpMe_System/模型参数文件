# OpMe_System 模型参数文件

更新时间：2026-06-19

## 默认模型与执行模式

- 默认模型路由：DeepSeek、Qwen、豆包。
- 默认 fallback：无密钥或调用失败时自动降级到离线规则。
- 默认输出：工程建议、诊断辅助、报告模板，不做现场安全最终裁决。

## 关键参数域

| 参数域 | 当前原则 |
| --- | --- |
| 后端 | FastAPI |
| 前端 | React + ECharts |
| 存储 | SQLite 历史库 |
| 报告 | PDF 自动生成与手动重生成 |
| 模型配置 | 通过模型设置管理，不提交 secrets |
| 降级 | 模型不可用时使用离线规则 |

## 模型与数据要求

- 模型调用日志必须记录 provider、模型名、调用状态、错误和降级路径。
- 工程建议需区分事实输入、规则判断、模型推理和人工复核要求。
- 对高风险施工建议默认增加安全提醒。

## 不允许保存或提交

- API key、客户机密、现场敏感原始资料、`.env`。
- 未经脱敏的企业设备数据和个人信息。
