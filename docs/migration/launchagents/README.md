# LaunchAgent Migration Copies

These `.plist` files are copies from the old Mac for migration reference only.
They preserve command shape and scheduling metadata, but their absolute paths
must be reviewed before loading on a new computer.

Do not load them unchanged unless the new checkout path is identical to the old
Mac canonical root.
