# Memory Atlas v1.2 Remediation Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Turn the reopened Memory Atlas v1.2 source/governance increment into a real, usable, multi-viewport product with verified workflows, data parity and full GitHub recovery.

**Architecture:** Keep the existing React/Vite frontend, Python `atlasctl` control plane and local `.app` runtime. Add real-browser acceptance around user paths, expose only a narrow allowlisted local command bridge where execution is required, keep Cloudflare Pages read-only, and publish only after the local candidate, online candidate and recovery rehearsal prove the same release.

**Tech Stack:** React, TypeScript, Vite, Python 3, Playwright/Chromium, Cloudflare Pages/Access, Git.

## Global Constraints

- One run may complete at most one remediation phase.
- No GitHub push, app reinstall or Cloudflare deployment before R8.
- Work in the canonical `LinzeColin/CodexProject/OpenAIDatabase` checkout on local `main`; do not create a branch or PR.
- Preserve raw append-only and credential-exclusion boundaries.
- Automated acceptance must cover 1470x661, 1440x900 and 390x844; user visual confirmation is not a blocking gate.
- A validator may claim PASS only when it exercises the relevant rendered/runtime user path.

---

### Task 1: R1 Layout And Multi-Viewport Gate

**Files:**
- Modify: `apps/memory-atlas/src/styles.css`
- Modify: `apps/memory-atlas/package.json`
- Create: `apps/memory-atlas/scripts/validate_memory_atlas_v1_2_home_multiviewport.cjs`
- Create: `tests/test_memory_atlas_v1_2_home_layout_contract.py`

- [x] Add a failing contract asserting `.workspace` has a dedicated row for all five main children.
- [x] Add a failing Playwright gate for 1470x661, 1440x900 and 390x844 that checks pairwise element intersections, horizontal overflow, command palette clipping and first-screen visibility.
- [x] Fix grid rows and responsive command palette sizing without redesigning the information architecture.
- [x] Run the new contract, browser gate, TypeScript build and `git diff --check`.
- [x] Commit locally on `main`; do not push; stop before R2.

### Task 2: R2 Product Identity And Information Architecture

**Files:**
- Modify: `apps/memory-atlas/src/App.tsx`
- Modify: `apps/memory-atlas/src/styles.css`
- Modify: `apps/memory-atlas/src/i18n/zh-CN.ts`
- Extend: `apps/memory-atlas/scripts/validate_memory_atlas_v1_2_home_multiviewport.cjs`

- [x] Add failing browser assertions that Stage/Phase IDs, CLI snippets and English safety internals are absent from the default first screen.
- [x] Replace the legacy shell hierarchy with v1.2 release identity and user-question-first navigation while preserving existing views.
- [x] Keep machine details behind explicit disclosure controls.
- [x] Verify headings, scan order, keyboard focus and three viewports.
- [x] Commit locally; do not push; stop before R3.

### Task 3: R3 Real Command Palette Workflows

**Files:**
- Modify: `apps/memory-atlas/src/App.tsx`
- Modify: `scripts/install_memory_atlas_app.py`
- Modify: local runtime server template generated by `scripts/install_memory_atlas_app.py`
- Create: `apps/memory-atlas/scripts/validate_memory_atlas_v1_2_command_workflows.cjs`
- Test: `tests/test_memory_atlas_app_runtime.py`

- [x] Define an allowlisted local command API for the six accepted commands; reject arbitrary shell input and non-local origins.
- [x] Make Cloudflare Pages read-only and show a specific local-app handoff instead of fake execution.
- [x] Add E2E tests for sync ChatGPT, sync Codex, weekly report, proposals, personalization and ChatGPT prefill-only deep explore.
- [x] Verify no silent send, no credential access and actionable failures.
- [x] Commit locally; do not push; stop before R4.

### Task 4: R4 Proposal Approval Apply Rollback Workflow

**Files:**
- Modify: `apps/memory-atlas/src/App.tsx`
- Modify: `scripts/atlasctl.py`
- Modify: `scripts/build_memory_atlas_proposal_apply.py`
- Create: `apps/memory-atlas/scripts/validate_memory_atlas_v1_2_proposal_e2e.cjs`
- Test: `tests/test_memory_atlas_proposal_apply.py`

- [x] Add failing E2E cases for unauthorized apply, authorized apply, validation failure and rollback.
- [x] Add a user-visible approval surface with diff narrator and exact target-file scope.
- [x] Execute only approved allowlisted proposals through the local bridge and preserve a rollback point.
- [x] Prove raw targets are impossible and failure restores the pre-apply state.
- [x] Commit locally; do not push; stop before R5.

### Task 5: R5 Owner Daily Product Entry

**Files:**
- Modify: `apps/memory-atlas/src/App.tsx`
- Modify: `scripts/atlasctl.py`
- Create: `apps/memory-atlas/scripts/validate_memory_atlas_v1_2_owner_daily_e2e.cjs`

- [x] Add a failing browser test for starting owner-daily, viewing step results and reading an actionable failure.
- [x] Connect the UI entry to the allowlisted local profile runner.
- [x] Present human conclusions before machine details.
- [x] Verify retry, partial failure and no-write dry-run behavior.
- [x] Commit locally; do not push; stop before R6.

### Task 6: R6 P0 Visualization And Filtering

**Files:**
- Modify: `apps/memory-atlas/src/App.tsx`
- Modify: visualization components under `apps/memory-atlas/src/`
- Modify: `apps/memory-atlas/src/styles.css`
- Create: `apps/memory-atlas/scripts/validate_memory_atlas_v1_2_visual_workflows.cjs`

- [x] Inventory the required P0 views against the restored TaskPack and add one failing assertion per missing decision workflow.
- [x] Ensure every retained chart has a Chinese human question, action and evidence drill-down.
- [x] Implement source/time/project/task filters across the required views.
- [x] Verify interaction changes rendered state and charts are not static decoration at all three viewports.
- [x] Commit locally; do not push; stop before R7.

### Task 7: R7 Data Parity Raw Evidence And Recovery

**Files:**
- Modify: data/sync/build scripts under `scripts/`
- Modify: `data/public_raw/` and `data/raw_archives/` only through append-only commands
- Modify: `scripts/deploy_memory_atlas_cloudflare.py`
- Create: `scripts/audit_memory_atlas_snapshot_parity.py`
- Create: `scripts/audit_memory_atlas_github_recovery.py`

- [x] Add failing tests for non-empty raw manifest proof, original source restoration and local/Pages candidate snapshot equality.
- [x] Materialize authorized source archives with manifest/hash evidence without credentials.
- [x] Build one immutable release snapshot and require local runtime and Pages candidate to use its hash.
- [x] Rehearse recovery into a temporary clean directory using only tracked files.
- [x] Commit locally; do not push; stop before R8.

### Task 8: R8 Overall Acceptance And Single Final Delivery

**Files:**
- Modify: final delivery records only after all gates pass
- Modify: `scripts/audit_memory_atlas_goal_completion.py`
- Create: final multi-viewport, workflow, parity and recovery evidence under `机器治理/证据与日志/final_delivery/`

- [ ] Run all 58 requirement gates and require every row to be VERIFIED.
- [ ] Run three-viewport screenshots, overlap/pixel checks and all critical user workflows on the release candidate.
- [ ] Prove raw safety, credential exclusion, snapshot parity and clean-directory recovery.
- [ ] Make the single final push to GitHub `main`, with no branch or PR.
- [ ] Reinstall both app entries, deploy the exact pushed commit, verify Access and rerun the same browser suite online.
- [ ] Clean only reproducible caches, verify `HEAD == origin/main`, port release and zero open PR/branches, then and only then mark the goal complete.
