# 00 快速入口

## 结论

这是 Memory Atlas v1.2 的人类入口。当前阶段是 S01 P2：双平面已经创建，
但需求冻结还没有写入；下一步是 S01 P3。

本文件不是跳转页。它直接说明现在应该如何理解这次升级、哪些内容已经完成、
哪些边界仍然不能碰。

## 当前阶段

- 已完成：S01 P1 现状核验。
- 已完成：S01 P2 双平面创建。
- 未完成：S01 P3 需求冻结。
- 未完成：S01 整体复审。
- 未完成：S02-S14。

## 你现在应该知道的三件事

1. v1.2 目标是把 Memory Atlas 升级成全中文、高信息 ROI、Anthropic 风格、
   多维行为智能和自我迭代系统。
2. ChatGPT、Codex、后续其他 agent 的 transcript 可以在用户授权后进入公开
   GitHub，但必须通过后续 source registry 和 raw append-only gate。
3. 凭证不是 transcript。cookies、session tokens、passwords、API keys、
   private keys、OAuth tokens 和浏览器凭证库永远不能提交。

## 本阶段边界

- No GitHub main upload in this phase。
- 不重装 app。
- 不移动 `apps/`、`scripts/`、`tests/`、`config/`、`data/`、`docs/governance/`。
- 不写 `v1.2需求冻结清单`；该文件属于 S01 P3。
- 不修改 raw archive。

## 下一步

下一步只允许进入 S01 P3：需求冻结。S01 P3 需要把四线范围、14 Stage 执行规则、
raw 公开授权、凭证排除和后续其他 agent 数据源扩展规则写成机器可验证配置。
