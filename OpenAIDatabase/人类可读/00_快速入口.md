# 00 快速入口

## 结论

这是 Memory Atlas v1.2 的人类入口。当前阶段是 S01 Review：S01 整体复审已通过，
S01 P1 现状核验、S01 P2 双平面创建和 S01 P3 需求冻结已经全部完成并通过复审。

本文件不是跳转页。它直接说明现在应该如何理解这次升级、哪些内容已经完成、
哪些边界仍然不能碰。

## 当前阶段

- 已完成：S01 P1 现状核验。
- 已完成：S01 P2 双平面创建。
- 已完成：S01 P3 需求冻结。
- 已完成：S01 整体复审。
- 未完成：S02-S14。

## 你现在应该知道的三件事

1. v1.2 目标是把 Memory Atlas 升级成全中文、高信息 ROI、Anthropic 风格、
   多维行为智能和自我迭代系统。
2. 旧隐私边界已被 v1.2 替换。用户授权后，ChatGPT、Codex、后续其他 agent 的 transcript 可以明文公开进 GitHub，
   但必须通过 source registry 和 raw append-only gate。
3. 凭证不是 transcript。cookies、session tokens、passwords、API keys、
   private keys、OAuth tokens 和浏览器凭证库永远不能提交。

## 本阶段边界

- No GitHub main upload in this review。
- 历史 phase 边界仍保留：No GitHub main upload in this phase。
- 不重装 app。
- 不移动 `apps/`、`scripts/`、`tests/`、`config/`、`data/`、`docs/governance/`。
- 不执行 S02 work；下一步只是后续 run 的 S02 P1。
- 不修改 raw archive。

## 下一步

下一步只允许进入 S02 P1。S02 P1 需要在新 run 中处理数据源模型与 source registry
的具体推进；本 S01 Review 不进入 S02，也不上传 GitHub main。
