# Changelog

## 1.0.0 - Portable Agent Memory V1 (`TSK.OpenAIDatabase.PAM1.0019`)

- Added one deterministic production-acceptance gate that reruns the existing five transport profiles, complete 160-case benchmark, security, performance, fault, Automation C E2E and clean-room recovery evidence without adding a second fixture corpus.
- Bound Discovery, Read, Retrieval, Mutation, Forgetting and Recovery to explicit machine-verifiable proof sources. The gate requires one canonical writer, zero legacy independent writers, five passing profiles and every existing quantitative hard gate.
- Added a published-mode verification path that fresh-fetches the exact accepted `main` commit, requires zero repository-hygiene violations and successful governance/OpenAIDatabase/Memory Atlas contract checks, requires the version tag to resolve to it, requires public `LinzeColin/AgentDatabase`, downloads the exact commit-pinned snapshot asset, and rejects any asset that is not commit-only, public-authorized, credential-free and redacted-summary-only before requiring final PR/Issue/non-main counts `0/0/0`.
- The retained raw-import wrapper and cutover replay tool are registered, tested and fail closed; the final reference scan found no proven-unused shim to remove. Direct/force push, history rewrite, archive branches and unsigned-authenticity claims remain prohibited.
- Current status is a local release candidate, not a production release. GitHub verifies the intended target `LinzeColin/AgentDatabase` remains `PUBLIC`; no new asset has been uploaded. Publication remains stopped until the integrated remote main is repository-hygiene clean.

## Unreleased - Memory-only snapshot and clean-room recovery (`TSK.OpenAIDatabase.PAM1.0018`)

- Added a commit-pinned, deterministic ZIP_STORED snapshot CLI for export, per-file SHA-256 validation, exact offline active-record queries and atomic restore into a new destination. It rejects unsafe member paths, symlinks, duplicates, encryption, credentials, tampering, missing files and wrong expected commits before publication.
- The snapshot contains canonical schema/records, generated Agent entrypoints and the embedded runtime/policy/runbook. Only exact repository-relative authorized public raw references are eligible; the current snapshot includes zero raw files and never resolves or copies 198 external/opaque source references.
- A clean-room drill reproduced all 12 commit-origin files byte-for-byte, preserved the canonical dataset hash at 100%, passed five snapshot plus five restored queries, and rejected all three required negative cases. Measured local recovery stayed below the 30-second drill bound and the 1,800-second RTO target.
- No archive branch, whole-repository bundle, Release asset, dependency or canonical/raw business write was created. The unsigned SHA-256 evidence proves integrity, not authenticity, and final public-safe asset publication remains exclusively assigned to `.PAM1.0019` after repository hygiene is green.

## Unreleased - Memory input and workflow supply-chain security (`TSK.OpenAIDatabase.PAM1.0012`)

- Added one fail-closed security policy and validator for raw evidence, pull-request titles/bodies and external documents. These boundaries are always `data_only`; NFKC injection checks, credential patterns and conservative entropy detection run before raw writes.
- Rejections return static reason codes and aggregate categories only. The bounded ten-case corpus records instruction obedience `0`, credential block rate `100%` and suspected-value echo `0`; unsafe paths, symlinks and archives retain their existing pre-write gates.
- Delegated workflow proof to the canonical root audit: all third-party Actions remain allowlisted full commit SHAs with traceable resolution sources, while trusted Settlement checks out no PR code and consumes no PR artifact/cache.
- Bound the security policy hash into the generated Agent handshake and documented revoke/rotate-first incident handling. Automatic Git-history rewrite is forbidden; the full 160-case benchmark remains `.PAM1.0013`.

## Unreleased - Memory forgetting and evidence-aware refusal (`TSK.OpenAIDatabase.PAM1.0011`)

- Current answers now consume only effective `active` records. Retired, disputed, candidate, expired and not-yet-valid records are excluded by default; `--include-inactive` remains audit-only and never authorizes an answer.
- Every query emits a deterministic retrieval decision. Missing evidence, unresolved conflict, stale truth or unverified candidates return `UNKNOWN`, abstain and name the missing conditions; explicit `--as-of` evidence is historical-only.
- `negative_trigger` requires explicit-user or hash-bound repository authority, verified state, a non-empty boundary and explicit validity. Silence cannot create negative memory or a positive opposite assertion.
- Added FAMA and abstention acceptance gates, schema/mutation enforcement and hash-bound Agent instructions. Retirement performs retrieval exclusion, not Git history erasure; canonical/raw business records and remote state remain unchanged.

## Unreleased - Memory lifecycle and bitemporal truth (`TSK.OpenAIDatabase.PAM1.0010`)

- Added deterministic exact/normalized/linked-overlap/same-key classification with NFKC, casefold and whitespace collapse; embeddings cannot uniquely decide or resolve conflicts.
- Updates now append recorded lifecycle transitions, retire the prior active record, preserve half-open valid intervals and reject normalized no-fact updates. Reversal remains an explicit update and stale concurrent targets fail before write.
- `memory.py query` now distinguishes current, valid-time `--as-of`, and `--recorded-as-of` audit modes. Legacy supersession derives its transition time from the successor or abstains when history cannot be bounded.
- Any duplicate, overlapping validity issue or unresolved conflict blocks automatic settlement. The 198-record canonical baseline remains clean, generated views remain below startup budgets, and canonical/raw business data and remote state are unchanged.

## Unreleased - Governed memory mutation admission (`TSK.OpenAIDatabase.PAM1.0009`)

- Added one strict mutation envelope/schema/policy and the unified `memory.py mutate` path for `add`, `update`, `retire`, and `dispute`; every request binds actor, source, source/authorization/valid time, scope, exact base SHA, sensitivity and a content-derived idempotency key.
- Explicit-user and hash-bound repository evidence are the only persistent sources. Raw import remains evidence-only and model inference persistence is zero; missing authorization, stale base, main-branch apply, key/content reuse, unauthorized scope and credential-shaped content fail before canonical writes.
- Candidate apply is atomic, single-flight and restricted to its deterministic `automation-c/memory-*` branch. Replayed add/update outcomes write no second record, and the PR plan emits provenance/content hashes without statement text.
- Added concise ChatGPT/Codex instructions and bound the schema, policy and instructions hashes into the one-object handshake. Local admission is complete; live branch/PR/CI/Settlement E2E remains `.PAM1.0017` and production acceptance remains `.PAM1.0019`.

## Unreleased - Five-profile Agent transport compatibility (`TSK.OpenAIDatabase.PAM1.0008`)

- Added one five-profile registry, a read-only compatibility harness and minimal client instructions for ChatGPT GitHub App, Codex Git, GitHub MCP, REST/HTTPS and local/offline snapshot hosts.
- All profiles normalize `discover/read/query/cite/freshness/fallback` onto the same seven-field memory identity, reject stale/tampered/unpinned inputs, declare bounded fallbacks and never write or scan the repository.
- Bound the registry, instructions and harness hashes into the generated handshake. Candidate-snapshot compatibility is complete; published live-memory validation remains explicitly assigned to `.PAM1.0019`.

## Unreleased - Generated Agent memory views (`TSK.OpenAIDatabase.PAM1.0007`)

- Added deterministic generated-only `data/memory/AGENT_MEMORY.md` and `data/memory/agent-memory.json` views from canonical high-importance active records; current sizes are 3,741 and 5,069 bytes against 24/256 KiB gates.
- Default startup now discovers one machine handshake object, then follows explicit compact/shard paths. The unique `LINZE_AGENT_MEMORY_V3` marker, source commit and schema/manifest/shard/compact hashes avoid repository scans and unbound copies.
- Extended `memory.py build --agent-views` with plan/check/guarded apply, idempotent replay, single-flight and manual-edit drift repair. Views exclude raw source refs and remain non-editable derivatives; canonical/raw business bytes are unchanged.

## Unreleased - Unified governed memory CLI (`TSK.OpenAIDatabase.PAM1.0006`)

- Added one offline `scripts/memory.py` entrypoint for `validate/build/query/import/export/doctor/benchmark/apply`; current build/export bytes reproduce the existing 198-record canonical dataset SHA-256.
- Query now supports `id/key/kind/scope/tag/as-of/keyword` with active-only default. Read commands write nothing and require neither a database nor a resident service.
- Every CLI write is plan-first and requires `--apply`, an exact Git `base SHA`, a content-bound idempotency key and a single-flight lock. Identical replay is a no-op; differing existing canonical mutations remain fail-closed until `.PAM1.0009` supplies admission and PR transaction semantics.
- Retired the historical cutover CLI writer and routed authorized raw import through the unified owner. `MOD-016`, `FORM-016`, `PARAM-102`, seven focused regressions and the fast/security tiers verify byte identity, query time, path/encoding, base/idempotency, atomic rollback and old independent writer count zero. Canonical/raw business bytes and remote state were unchanged.

## Unreleased - Governed raw evidence import (`TSK.OpenAIDatabase.PAM1.0005`)

- Made `data/public_raw/YYYY-MM/` the only destination for new public raw text while retaining the 513 historical files in their append-only compatibility paths without moves or rewrites.
- Added a plan-first, atomic and idempotent importer that binds an exact source SHA-256 to explicit publication authorization, splits sanitized UTF-8 into at most 900 KiB parts, and writes an adjacent source/authorization/hash sidecar.
- Raw input remains inert evidence: prompt/script injection, traversal, symlinks, archive input, Unicode bidi/invisible controls, credentials, oversize input, collisions and part-hash drift fail closed. Its V2 source contract permits `raw_import` candidate only and performs zero automatic active promotion.

## Unreleased - Canonical memory cutover (`TSK.OpenAIDatabase.PAM1.0004`)

- Migrated all 278 profiled legacy IDs into one audited outcome: 198 canonical V2 records and 80 raw-evidence-only dispositions. Canonical status counts are active `6`, candidate `108`, retired `84`, disputed `0`.
- Added a deterministic, fail-closed, atomic and idempotent cutover writer. The tracked output is one 488,877-byte shard plus one manifest; schema, source refs, legacy-row hashes and record hashes cover 198/198 records.
- Switched the sharding planner, read-only search/fetch index, Memory Atlas builder and three-layer context map to the canonical manifest. The former active/candidates/curation/secret-ref trees remain byte-locked read-only evidence with zero legacy writer and zero dual-write paths.
- Added `MOD-014`, `FORM-014`, `PARAM-099..100`, six cutover regressions and updated focused consumer tests. Raw-evidence deletion remains `.PAM1.0005`; governed mutation CLI remains `.PAM1.0006`.

## Unreleased - Legacy memory quality profile (`TSK.OpenAIDatabase.PAM1.0003`)

- Added a deterministic, fail-closed profiling contract and builder for active/candidate/curation/secret-ref legacy inputs. The builder supports no-write dry-run and atomic writes limited to one redacted migration map and one full CI-artifact quality report.
- Profiled 278 unique IDs across 1,980 record occurrences with 100% source/date/recorded-time/evidence coverage. Exactly-one dispositions are: active `6`, candidate `108`, raw-evidence-only `80`, retire `84`, prohibited `0`, owner-decision `0`.
- The 586,685-byte map and 9,101-byte report rebuild byte-identically; neither contains statement/source/evidence/PII values. Credential IDs, material conflicts, absolute source refs and canonical memory writes are all zero.
- `MOD-013`, `FORM-013` and `PARAM-096..098` document disposition precedence, stable proposition/scope hashing, a fixed profile date, report-only freshness buckets and one minimum-disclosure sample per non-empty class. Actual records migration and consumer cutover remain exclusively assigned to `.PAM1.0004`.

## Unreleased - Deterministic canonical memory sharding (`TSK.OpenAIDatabase.PAM1.0002`)

- Added a 900 KiB `records-NNNN.jsonl` contract and one generated direct-path discovery manifest contract. `MOD-012`, `FORM-012` and `PARAM-093..095` document the rotation/hash formula, 921600-byte hard limit, conservative API comparison and one-object discovery budget.
- Added a read-only planner that normalizes JSON to NFC/LF/compact sorted-key bytes, sorts unique record IDs, produces ordered shard/dataset SHA-256 evidence and verifies unique reconstruction without echoing record content.
- Boundary and corruption regressions accept 899/900 KiB, reject >900 KiB, duplicate/truncated/duplicate-key/reordered/tampered inputs, and prove repeated builds byte-identical.
- The unchanged 1,082,592-byte/278-record legacy input plans to two shards (918,848 and 135,054 bytes). No canonical data, legacy writer/reader, database, service, dependency, deployment or remote business state changed.

## Unreleased - Memory Record Schema V2 contract (`TSK.OpenAIDatabase.PAM1.0001`)

- Added one Draft 2020-12 `config/memory.schema.json` contract covering the required identity/key, source, bitemporal, lifecycle, scope, supersession/conflict, verification, negative-trigger, sensitivity and deterministic SHA-256 fields. Active records must be verified and conflict-resolved; `model_inference` cannot be active; credential presence is schema-invalid.
- Added a machine-verifiable V1→V2 migration contract that gives every one of the 32 legacy schema/active-record fields exactly one disposition and assigns ambiguous per-record profiling to `.0003`, sharding to `.0002`, and actual data/consumer cutover to `.0004`.
- Added four valid records and six invalid cases covering schema, hash and dataset boundaries. No active/candidate/raw data, dependency, database, service, runtime reader/writer, model/formula/parameter value, deployment or business remote state changed.

## Unreleased - Project CLEAN Gate (`TSK.OpenAIDatabase.CLEAN1.0010`)

- Recalculated directory/writer, command/dead-script, generated/transient, raw/security, verification/CI, governance, large-object and context-budget gates from an independent shallow clone with no shared object alternates.
- Built and tested twice: both full verification rounds passed 253/253; two commit-time data builds and two frontend builds produced matching SHA-256 trees; normal build/test left tracked status clean.
- Fixed one fresh-CI ordering defect found by the gate: frontend build now precedes the integration tier, and verification policy fails closed on future step-order drift.
- Compressed project `AGENTS.md` from 7,441 to 5,852 bytes while retaining raw credential, private archive, routing, proposal-only and generated-candidate boundaries. No product feature, dependency, model/formula/parameter value, data, deployment or business remote state changed.

## Unreleased - Verification and reproducible build convergence (`TSK.OpenAIDatabase.CLEAN1.0009`)

- Added one fail-closed verification policy and runner that assign all 52 Python test files to exactly one fast, unit, security or integration capability owner; `full` aggregates those tiers once. Duplicate tests, conditional skips, known flaky retries, implicit unit live effects, unowned frontend validators, dependency/lock drift, duplicate configs and nested workflows are all hard-zero gates.
- Preserved both active Stage 8 command names while replacing their duplicate npm command bodies with one canonical implementation plus one explicit compatibility delegation. Removed two obsolete conditional skip decorators without weakening any assertion.
- Reduced the root CI public-raw security scan topology from seven transitive executions to one aggregate goal-completion gate. Release, visual, acceptance, Cloudflare preflight and R8 final acceptance remain delegated and fail closed; the retained 452,781,269-byte scan passed once in 263.65 seconds.
- Bound Memory Atlas data-build time fields to `SOURCE_DATE_EPOCH` in CI; two independent builds from the same epoch produced the identical SHA-256. A clean temporary environment installed 154 locked packages and passed TypeScript lint plus Vite build. No dependency version, product model/formula value, live deployment or GitHub business state changed.

## Unreleased - Raw material emergency remediation (`TSK.OpenAIDatabase.CLEAN1.0008`)

- Converged tracked raw data to the single shallow `data/public_raw` destination and retired repository session/raw tar, ZIP, bundle, split-part and encrypted-copy producers.
- Reconciled six legacy archive dispositions against private `LinzeColin/AgentDatabase` Release asset IDs, sizes and SHA-256 digests before removing the public tip copies.
- Removed the public session archive and its plaintext access note. Context-aware no-echo matching found three sensitive-line occurrences across three public-raw JSONL files; all were replaced with `[REDACTED_CREDENTIAL]`, the Chinese password-label detector was added, and all three manifest/ledger hashes were reconciled with drift zero.
- Added fail-closed credential/no-echo, raw instruction trust=`none`, public visibility, depth, private-origin, disposition and no-new-bundle gates. The owner-authorized security-only history rewrite is verified on public main and affected owner-managed tags at `5e85e7f`; GitHub's 272 internal pull refs and cached views remain an explicit Support-only purge boundary.

## Unreleased - Lean Governance convergence

- Rebuilt the three Chinese human entries deterministically from canonical facts and removed repeated presentation-only detail while retaining Stage → Phase → Task, hours/progress, Gate, Acceptance, evidence, result and rollback coverage.
- Registered and hash-locked 14 still-consumed legacy governance views as read-only compatibility artifacts; their former dashboard writer now emits no OpenAIDatabase project outputs.
- Removed stale task-state prose from stable README/AGENTS entry points. No legacy file was deleted or moved, and product runtime, private data, model/formula/parameter values, package version and GitHub state are unchanged.

## Unreleased - ID Governance V2 migration

- Migrated OpenAIDatabase mutable Task, Acceptance and Event references to project-scoped V2 IDs while preserving 87 legacy identifiers as exactly-one aliases.
- Added fail-closed migrated-project alias auditing, an atomic/idempotent migration transaction, deterministic legacy-event rendering and focused regression coverage.
- Historical append-only events, commits, manifests and evidence paths remain unchanged; product runtime, model/formula/parameter values, private data and deployment state are unchanged.
- This task is locally accepted only. GitHub `main` publication remains deferred until all 37 Task Pack tasks are complete.

## Unreleased - Archive offload (bomb A)

- 将 5 个大型运行时归档（101 个 90MB 分片，共 6.08GB）迁至私有 AgentDatabase 的 Release，全部 sha256 逐字节校验；原位留迁移说明。数据零丢失，仓库摆脱大文件膨胀。

## Unreleased - OpenAIDatabase Shared Memory RUN 1

- Extended the existing personalization pipeline to deterministically generate ChatGPT, Codex, and Claude projections from one canonical source set, with a shared `bundle_id`, `canonical_source_hash`, and provider-neutral manifest.
- Added a minimal `claude_personalization` route, a generated Claude projection capped at 4096 bytes, and focused coverage for shared identity, deterministic reruns, source-change invalidation, generated-only artifacts, and raw/private path rejection.
- Independent final review found that bundle identity omitted auxiliary projection inputs and evaluator trusted manifest source proof. The premature accepted marker was revoked; v2 projection-input provenance and fail-closed source-proof recomputation were added, 22 targeted tests plus all renderer/governance/CI/diff gates passed, and a second independent review found no remaining P0/P1.

This RUN does not execute RUN 2 or RUN 3, read or write raw/private data, prove independent real-agent consumption, commit, or push.

This file records the current release line. Detailed stage-by-stage history remains
recoverable from Git and is intentionally not duplicated here.

## Unreleased - Memory Atlas v1.2.1

### Current delivery state

- Initiative: `Memory Atlas v1.2.1`, ST00-ST16.
- Machine Roadmap: S01-S16, 50 phases, 149 Tasks; ST00 is the external intake wrapper.
- Current completed Task count after this change: `19/149`.
- Current repository package version remains `OpenAIDatabase/VERSION = 0.2.0`.
- Parent baseline: `8329d94b7b2068c6a8d01b3b7edce52c47b10113` on `main`.
- Delivery remains local-only: one final GitHub main upload occurs only after all Tasks,
  final review, review remediation and final recovery proof pass.
- Current boundary: no intermediate branch, PR, push, deploy, app reinstall or cache
  cleanup.

### Root Workflow consolidation (`TSK.CodexProject.REPO1.0005`)

- Merged the non-executable nested Memory Atlas CI role into the repository-root
  `openai-database-ci.yml` workflow and removed the nested workflow path.
- Preserved the OpenAIDatabase unit, generated-drift, Memory Atlas build, release,
  visual, acceptance, goal-completion, and offline Cloudflare preflight gates.
- No OpenAIDatabase model, formula, parameter value, product version, data, runtime,
  deployment, or delivery-readiness claim changed; remote Actions proof is deferred
  until the complete 37-Task package is published.

### Repository hygiene (`TSK.CodexProject.REPO1.0006`)

- Added a fail-closed 1 MiB tracked-object gate with immutable baseline exceptions,
  owner/purpose/consumer/retention/recovery metadata, archive/runtime-noise checks,
  and whole-repository backup-producer detection.
- Stopped scheduled raw-session tar generation and commit targeting. Direct raw
  exports now require an explicit destination outside any Git worktree.
- Removed one numeric-token transfer tar only after the extracted data matched its
  recorded SHA-256 values. The sole 38.3 MB June session tar remains baseline-locked
  until an independent private recovery copy is verified.
- Product version, model/formula/parameter values, deployment and readiness are
  unchanged. History rewrite and remote publication remain deferred.

### S03-P1-T2 - Current owner files

- Replaced 9,259 lines of append-only stage narration in `CHANGELOG.md`,
  `开发记录.md` and `模型参数文件.md` with bounded current-state summaries.
- Preserved the current v1.2 R8 release evidence, v1.2.1 Task progress, active formulas,
  active parameter values, security boundaries, validation commands and recovery route.
- Kept historical detail in Git instead of creating an archive, legacy or history copy.
- Did not delete source, documentation, raw data, source packages or generated data.
- Did not change runtime code, validators, package configuration or deployment state.
- Did not push or deploy; the local atomic commit is retained for the final overall upload.

### S01 and S02 audit closure

- S01 completed 9 Tasks covering Git/version/runtime baselines, lineage boundaries,
  retained/replaced/deprecated surfaces, quality debt, completion gaps and S02 scope.
- S02 completed 9 Tasks covering full-tree inventory, cleanup candidates, maintenance
  hotspots, reference evidence, disposition, Git recovery, cleanup batches, protected
  paths and the exact S03 planning surface.
- S02 reports are machine-local audit evidence and are not treated as product completion.
- The v1.2.1 package source remains external and hash-pinned; its Task Pack must not be
  copied into the repository during implementation.

### S03 deletion boundary

- `S03-P1-T1` is not complete.
- The planned document deletions are deferred because the audited candidates remain
  referenced by current validators, owner files or recovery entry points.
- The user permits deletion only for files exclusive to the current Task; shared or
  active files and cache cleanup wait for the final cleanup review.
- No deletion candidate becomes eligible solely because a broad project goal exists.
- Human-document consolidation, governance/generated cleanup, dead-reference repair,
  cleanup manifest, full regression and final S03 delivery remain pending.

### Validation and recovery

- Current Task validator: `/tmp/validate_memory_atlas_s03_p1_t2.py`.
- Historical owner files: `git log --follow -- OpenAIDatabase/<owner-file>`.
- Restore a prior file: `git show <commit>^:OpenAIDatabase/<owner-file>`.
- Canonical v1.2 release evidence:
  `机器治理/证据与日志/remediation/v1_2_r8/status.json` and
  `机器治理/证据与日志/remediation/v1_2_r8/final_acceptance.json`.
- Canonical v1.2 source recovery:
  `docs/source_packages/memory_atlas_v1_2/SOURCE_MANIFEST.json`.

## Memory Atlas v1.2 - Current predecessor release

- Machine status:
  `R8_ACCEPTANCE_AND_LIVE_DELIVERY_VERIFIED_PENDING_SINGLE_FINAL_PUSH`.
- R8 records 17/17 release gates, 58/58 requirements, 4/4 upgrade lines and 14/14
  verified v1.2 stages at runtime commit
  `12734c10bf37ee7afc86d62f72e70369a1bcd732`.
- The accepted snapshot records 435 nodes, 2,325 edges and 401 timeline items and uses
  three browser viewports: 1470x661, 1440x900 and 390x844.
- This predecessor evidence is not evidence that v1.2.1 is complete.

## 2026-07-16 - MacData main-only local migration

- `TSK.OpenAIDatabase.CLEAN1.0001` preserves the latest airM2/proM2 snapshots in the local `main` worktree and replaces persistent `macdata-*` producers with one exact-marker Automation C short-lived PR transaction per stage.
- Publication now requires exact head/base, trusted `github-actions` Settlement provenance, exact merge SHA, per-file SHA256 reconciliation, and terminal `PR/Issue/non-main=0/0/0`.
- 25 focused tests and both zero-write device simulations pass. GitHub publication, legacy branch deletion and formal remote acceptance remain deferred until the 37-task package gate.

## 2026-07-16 - Directory lifecycle and single token destination

- `TSK.OpenAIDatabase.CLEAN1.0005` assigns every tracked directory to one top-level owner and registers 17 unique default data destinations in `config/storage/directory_lifecycle.json`.
- Current numeric token telemetry moved byte-for-byte from `token_usage/current-mac-latest/` to `data/run_logs/token_usage/`; the exporter and sync staging now use only the canonical run-log destination.
- Obsolete `machine/` (10 files) and `文档/` (7 files) were retired after base fingerprint reconciliation. Active `机器治理/` and `人类可读/` remain unchanged because they have live product consumers.
- Historical token/session packages remain read-only until their separately scoped `.0007`/`.0008` gates. No raw/private payload, GitHub state, model value or product version was changed.

## 2026-07-16 - Canonical command surface convergence

- `TSK.OpenAIDatabase.CLEAN1.0006` records 20 canonical capabilities in `config/command_ownership.json` and routes active build/audit CI commands through `atlasctl`.
- Nine write-capable subcommands now fail closed unless callers choose `--dry-run` or `--apply`; 32 active implicit-mode calls across tests and the Memory Atlas app were migrated to explicit intent.
- Two zero-reference CJS entrypoints were removed after hash and caller verification. All 52 referenced implementation scripts remain in place, and the deprecated `deep-explore` alias contains no business logic.
- Focused regressions, command/lifecycle gates and the Memory Atlas production build pass. GitHub publication, model/formula/parameter values, private local data and product version remain unchanged.

## 2026-07-16 - Generated and local-state retention convergence

- `TSK.OpenAIDatabase.CLEAN1.0007` adds one generated retention registry and fail-closed validator covering tracked derived data, structured run ledgers, compact evidence, numeric telemetry, deferred session archives and ignored local state.
- The 10-file legacy numeric package and one tracked SQLite index were deleted after base SHA-256 reconciliation, consumer tracing and recovery verification; the ignored SQLite index now auto-builds from tracked JSONL when search/fetch needs it.
- A consumer regression identified two short evidence files still referenced by JSONL run ledgers. Both were restored byte-for-byte and explicitly retained; no consumer was changed to conceal the dependency.
- Twenty-four focused consumer tests, both retention/lifecycle validators and the TypeScript/Vite production build pass. The normal build leaves the Task diff and status hashes unchanged; private session payload, credentials, GitHub state, model/formula/parameter values and product version remain unchanged.

## 2026-07-16 - Independent 160-case memory Gold benchmark

- Added one fixed-seed synthetic `benchmark_v1.jsonl` with exactly 160 cases: eight capability categories with 20 cases each, 62 stale/retired traps, 28 abstention cases and hard negatives in every case.
- Added a strict Draft 2020-12 case schema, deterministic curator, separate read-only validator and five focused regressions. Duplicate, answer/sensitive leak and curator/approver/tested-algorithm role errors are zero.
- Kept real memory, raw evidence and local paths out of the Gold set. Machine validation does not claim human approval, and Recall/MRR/current-state/provenance/FAMA CI gates remain exclusively assigned to `PAM1.0014`.
- Corrected the prior credential attack fixture to store split fragments and join only at test runtime. Credential blocking remains 4/4 while tracked-repository privacy scanning returns zero hits; no allowlist or weaker security rule was added.
- Focused tests, Fast 19/19 and Security 111/111 pass. Canonical/raw business records, dependencies, services and GitHub business state are unchanged.

## 2026-07-16 - Deterministic memory evaluation required CI gate

- Added one label-isolated evaluator: prediction receives only `state/query/as_of`; Gold labels are reserved for scoring. No LLM judge, network request, full-tree scan or new sample corpus was introduced.
- Added fixed 16-case PR and complete 160-case main/schedule/manual suites. Both tracked reports pass Recall@5, MRR, current/temporal/update accuracy, provenance, abstention precision/recall and FAMA at 1.0, with critical stale use 0 and five transport profiles consistent.
- Added exact threshold/suite locks, failing threshold fixtures, report-hash drift checks and measured query p95/runtime gates. The full deterministic workload is 315035 input bytes and 6708 output bytes; dataset size is 475733 bytes.
- Wired fast/full commands into the existing read-only CI job without warning/soft-failure paths. Remote check execution remains deferred to the final whole-package publication; this Task performs no GitHub write.

## 2026-07-16 - Bounded lexical retrieval and conditional GitHub cache

- Added a deterministic NFKC/casefold lexical router with exact scope/alias terms,
  CJK 2/3-grams and Latin 3-grams. The generated 80,810-byte machine handshake
  indexes 6/6 public active records with 1,395 postings and remains below 256 KiB.
- Added an authenticated, commit-SHA-pinned GitHub Contents reader with ETag cache,
  `If-None-Match`, cached 304 replay, commit invalidation, serial bounded retries,
  `Retry-After` and `x-ratelimit-reset` handling. Official GitHub conditional,
  rate-limit and Contents API contracts were rechecked on 2026-07-16.
- Reused the existing 160 bilingual alias/noise Gold cases: indexed Recall@5 remains
  1.0, prediction parity is 160/160 and quality degradation is 0. Cold/warm content
  requests are 2/2, warm 304 transfers 0 bytes and recursive full-tree scans are 0.
- Added a required performance CI check and tracked report with current p50/p95
  observations plus deterministic source/request/quality gates. No new sample, vector
  database, custom MCP, resident service, dependency, canonical/raw record, live
  benchmark request or GitHub write was introduced.

## 2026-07-16 - Deterministic fault reliability and bounded recovery

- Added one compact 37-case offline matrix for corrupt JSONL/hash/shard/index input,
  stale ETag, 403/404/429/500/timeout transport, 2/5/10 concurrent writers, duplicate
  workflow/Settlement events, stale PR/base and Janitor active-versus-orphan routing.
- GitHub reads now retry only bounded transient server/time-out failures, preserve
  `Retry-After`, stop after three attempts and return stable forbidden/not-found/error
  codes. No retry path performs a write or reaches the live network in this suite.
- Canonical replacement now preserves a complete validated target if post-commit
  cleanup/fsync fails; pre-commit faults restore the prior canonical set. Idempotent
  retry converges with partial writes, duplicate records and duplicate Settlement writes 0.
- The tracked report passes 37/37 cases with one legal writer per profile and maximum
  modeled convergence 15 minutes. Live PR/CI/Settlement execution remains `.PAM1.0017`;
  no new business sample, dependency, canonical/raw record or GitHub write was added.

## 2026-07-16 - Governed memory Automation C seven-scenario E2E

- Added transaction glue that binds an admitted memory plan to one unique, redacted
  Automation C marker and reuses the trusted default-branch Settlement policy; raw
  statement/source text never enters the marker body.
- Added one compact seven-scenario evaluator using disposable synthetic local Git
  repositories. Add/update/retire/duplicate no-op squash-merge and delete; dispute
  closes/deletes; invalid and model-inference inputs stop before a branch or write.
- The tracked report passes 7/7 with new facts readable through all five profiles,
  update/retire old facts invisible 2/2, replay writes zero, and no partial data,
  duplicate record/PR/Settlement, daily queue, live network request, or target write.
- Every sandbox ends at PR/Issue/non-main branch `0/0/0`. This is the Task Pack's
  permitted sandbox/test-repository path; production live publication and independent
  five-profile acceptance remain `TSK.OpenAIDatabase.PAM1.0019`.

## Historical versions

Older v1.1.x, v1.2 stage, acceptance, review and remediation narratives are available
through Git history. They are not current release claims and must not be copied into a
new archive directory.
