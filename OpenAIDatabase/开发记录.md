# OpenAIDatabase / Memory Atlas 当前开发记录

本文件只保存当前可执行状态。旧版本、逐 Stage 和逐 Phase 记录通过 Git 历史恢复，
不再复制为新的 archive、legacy 或 history 目录。

## 当前快照

| 项目 | 当前事实 |
|---|---|
| 当前升级线 | Memory Atlas v1.2.1，ST00-ST16 |
| 仓库 | `LinzeColin/CodexProject` 的 `OpenAIDatabase` 子树 |
| 分支 | `main`，不创建开发分支或 PR |
| 本 Task 父基线 | `8329d94b7b2068c6a8d01b3b7edce52c47b10113` |
| OpenAIDatabase VERSION | `0.2.0`，本 Task 不修改 |
| Task Pack | 16 Stages、50 Phases、149 Tasks；ST00 为外部 intake wrapper |
| 已完成 | `19/149`，包含本次 `S03-P1-T2` |
| 当前 Stage | S03 部分完成；删除类 Task 延后 |
| GitHub 交付 | 整体任务完成、复审、问题修复后一次性上传 main |
| 清理 | 内存、缓存、临时存储只在所有任务完成后处理 |

## 事实优先级

1. 当前 Git tree、真实运行、测试和浏览器证据。
2. v1.2.1 Task Pack 的 machine Roadmap、scope、acceptance 和 stop conditions。
3. 当前机器治理配置、状态 JSON、公式和参数 registry。
4. 本文件与 `CHANGELOG.md`、`模型参数文件.md` 的 owner 摘要。
5. Git 历史中的旧阶段叙述；只能作为历史，不得覆盖当前证据。

旧字符串 validator、文件存在检查或历史 PASS 文本不能单独证明当前产品完成。

## v1.2 当前前置版本

- 当前 v1.2 machine status 为
  `R8_ACCEPTANCE_AND_LIVE_DELIVERY_VERIFIED_PENDING_SINGLE_FINAL_PUSH`。
- canonical evidence：
  `机器治理/证据与日志/remediation/v1_2_r8/status.json`、
  `机器治理/证据与日志/remediation/v1_2_r8/final_acceptance.json`。
- R8 记录 17/17 gates、58/58 requirements、4/4 upgrade lines、14/14 stages。
- runtime commit：`12734c10bf37ee7afc86d62f72e70369a1bcd732`。
- v1.2 证据是 v1.2.1 的输入基线，不代表 v1.2.1 已完成。

## v1.2.1 进度

| 范围 | 状态 | 完成数 | 说明 |
|---|---|---:|---|
| ST00 | VERIFIED_INTAKE | 外部 wrapper | ZIP 与解包内容 25/25 hash 一致 |
| S01 | COMPLETE | 9/9 | 基线、版本谱系、范围、质量债务和 S02 预算 |
| S02 | COMPLETE_PLANNING_ONLY | 9/9 | 全树只读审计、恢复证据、批次、保护和 S03 计划 |
| S03-P1-T1 | DEFERRED_NOT_ELIGIBLE | 0/1 | 删除候选不是 task-only，等待最终清理复核 |
| S03-P1-T2 | COMPLETE_LOCAL_ONLY | 1/1 | 三个 owner 文件收敛，无删除、无 push |
| S03 其余 Tasks | PENDING_OR_DEFERRED | 0/7 | 删除、引用修复、manifest、回归和交付尚未执行 |
| S04-S16 | NOT_STARTED | 0/122 | 按后续单 Task run 推进 |

总完成数为 `19/149`。S03 不满足 Stage Pass Gate，v1.2.1 不得显示 complete。

## S03-P1-T1 延后决定

用户限定：只有与当前 Task 独占相关的文件可以现在删除；共享、活跃或其他任务仍依赖
的文件，以及机器缓存，应等整体任务完成后的清理复核再处理。

复核结果：

- S02 精确计划中的 candidate `eligible = 0`，授权路径为 0。
- `S03-P1-T1` 语义候选共 183 个文档文件。
- 候选仍被当前 validator、owner 文件、machine evidence 或恢复入口引用。
- 4 份旧 v1.1.7 contract 和 1 份旧 release note 被当前 validator 直接读取。
- v1.2 R0-R7 文档仍被 current review、owner 文件和实现计划引用。
- 这类依赖不允许用用户的受限批准自动改写成“仅与本 Task 相关”。

因此本轮删除数为 0；不清理 `/tmp`、`.codex-tmp`、cache、raw、source package 或
其他项目文件。`S03-P1-T1` 保持未完成，不计入进度。

## S03-P1-T2 实施记录

目标：将 `CHANGELOG.md` 收敛为版本级变更，将 `开发记录.md` 收敛为当前开发状态，
将 `模型参数文件.md` 只保留当前有效模型、公式、参数和权威 registry 路由。

修改范围：

- `OpenAIDatabase/CHANGELOG.md`
- `OpenAIDatabase/开发记录.md`
- `OpenAIDatabase/模型参数文件.md`

明确未修改：

- WDA 的 5 个既有用户修改。
- OpenAIDatabase runtime、frontend、scripts、tests、package 和 deployment config。
- raw、public_raw、raw_archives、private imports 和 source packages。
- GitHub、Cloudflare、已安装 app 和本机缓存。

压缩预算：

| 文件 | 原始行数 | 最大行数 | 最大字节 |
|---|---:|---:|---:|
| CHANGELOG.md | 3,247 | 500 | 80,000 |
| 开发记录.md | 2,825 | 300 | 60,000 |
| 模型参数文件.md | 3,187 | 500 | 80,000 |

合计原始 9,259 行；目标最多 1,300 行。历史通过 Git 恢复，不创建副本。

## Task Pack 与恢复

- 原始 ZIP：`v1.2.1_四线16Stage质量收敛升级_TaskPack.zip`。
- SHA-256：`db59f4db3ac02845fb3cde346a301cba64146c81c7ba19168f52c27e82ddd0f1`。
- Task Pack machine policy 明确 `taskpack_should_not_be_committed = true`；实施期间保持
  外部 hash-pinned source，不把 ZIP 或解包副本写进仓库。
- v1.2 source recovery 继续由
  `docs/source_packages/memory_atlas_v1_2/SOURCE_MANIFEST.json` 管理。
- 三个 owner 文件的旧正文可用
  `git show 8329d94b7b2068c6a8d01b3b7edce52c47b10113:<path>` 恢复。

## 验证合同

本 Task 在本地提交前必须通过：

```bash
python3 /tmp/validate_memory_atlas_s03_p1_t2.py
git diff --check
git diff --name-status 8329d94b7b2068c6a8d01b3b7edce52c47b10113 -- OpenAIDatabase
git diff --cached --check
```

验证必须证明：只有三个 owner 文件发生修改、没有删除、行数与字节预算通过、历史
可恢复、关键 v1.2.1/R8/公式参数事实存在、raw/source package/runtime 零变化。

本次提交前实测：独立 validator PASS；Task Pack/Roadmap/R8/JSON/YAML 参数语义对照
PASS；`git diff --check` PASS；新增行高风险 secret pattern 扫描 PASS；删除文件数为
0；实时 remote main 与固定父基线一致。未运行 S03 全量回归，因为 dead-reference、
validator 收敛和完整回归分别属于后续 `S03-P2-T2`、S04 和 `S03-P3-T1`。

## 风险与后续

- 历史 validator 可能仍读取被收敛掉的阶段文本；S04 负责将验证体系改成当前行为与
  machine contract，不得为了旧字符串测试恢复数千行历史。
- S03 删除类 Tasks、dead-reference repair、cleanup manifest、完整回归和 S03 Pass
  Gate 均保留为未完成。
- 按用户的清理时序，下一项可独立执行的 Task 是 `S04-P1-T1`；S03 未完成项在最终
  清理复核中恢复处理并重新验收。
- 本 Task 只允许本地原子 commit；不 push、不 deploy、不重装 app、不清缓存。
