# airM2 macdata 开发记录

## 2026-07-05 初始化接入

目标：把 `macdata_airM2_codex_automation_taskpack.zip` 安装到 `LinzeColin/CodexProject/OpenAIDatabase/macdata/airM2`，并按原始任务包语义运行。

已解决：

1. 确认当前机器为 `MacBook Air / Mac14,2 / Apple M2 / 8GB`。
2. 确认 GitHub 主仓库为 `https://github.com/LinzeColin/CodexProject.git`，目标目录为 `OpenAIDatabase/macdata/airM2`。
3. 确认归档分支为 `macdata-airM2`。
4. 新增 `owner_confirmations.json`，记录用户确认，不包含凭证。
5. 修复上一轮错误扩展：恢复“禁止自动清理 Docker/Homebrew/系统缓存/项目缓存”的原始任务包语义。
6. 恢复原始任务包测试，不保留项目缓存清理扩展测试。
7. 创建并启用 Codex Automation：`macdata-airm2-daily-controlled-archive`。
8. 完成一次完整受控运行并创建 `macdata-airM2` 归档分支。
9. 曾按用户要求将全中文报告文件从 `.md` 改为 `.txt`；2026-07-06 已按新要求回退，目标最新报告恢复为 `reports/latest/latest_report.md`。
10. 增加 stale latest 清理：写入 `latest_report.md` 前删除旧 `reports/latest/latest_report.txt`，避免 latest 目录同时出现 md/txt 双入口。

风险与管控：

1. GitHub 分支不存在：由首次 push 自动创建，随后用远程 hash 验证。
2. 清理误删风险：macdata 任务只允许清理自身 3 天以前记录和临时上传缓存；电脑大清理必须另开独立任务。
3. 凭证泄露风险：上传前扫描 raw/report/status；命令输出会按高风险模式脱敏。
4. 设备不匹配风险：预检失败时停止。

验收标准：

1. 单元测试通过。
2. 预检通过。
3. `main` 上任务包 commit + push 成功。
4. `macdata-airM2` 归档分支创建或更新成功。
5. 远程 hash 与本地提交 hash 一致后，才允许清理旧本机 macdata 数据和 macdata 临时缓存。
6. 全中文报告文件必须以 `.md` 形式生成、上传和监控。

最终证据：

- 本轮回退代码提交：`7afdce529206652b7a9aac8ce5d4dbcc18e884ef`。
- GitHub `macdata-airM2`：`c6679c425cee44b31e0fc02d7ba0b7481a9e4932`。
- 本次 raw commit：`0d8037b200fb67e653cf6d2abf44e7456ca2c45f`。
- 本次 report commit：`c6679c425cee44b31e0fc02d7ba0b7481a9e4932`。
- 最新本机报告：`reports/latest/latest_report.md`。
- 远端 latest 验证：`reports/latest/` 只保留 `.gitkeep` 与 `latest_report.md`，无 stale `latest_report.txt`。
- 验证：airM2 单元测试 4 tests OK；proM2 单元测试 8 tests OK；preflight ok true；完整 `--execute` 成功并远程验证 True。

## 2026-07-16 主分支单一化迁移

任务：`TSK.OpenAIDatabase.CLEAN1.0001` / `ACC.OpenAIDatabase.CLEAN1.0001`。

已完成：

1. 将 airM2 旧分支最新 17 个快照文件原样迁入本地 `main` 工作树，聚合 SHA256 为 `9f0484dccd4a9e1ee684541fe59804425cda3a93a5049c0e552c4f5967bada15`。
2. 将长期 `macdata-airM2` 推送改为短命 PR 事务：唯一分支、精确 base/head、凭证扫描、Settlement 等待、逐文件哈希复核和失败补偿。
3. 生产者在运行前后强制 `PR/Issue/non-main=0/0/0`，拒绝 legacy 分支存在、direct-push `main`、Issue 变更和非本事务删除。
4. 新增本地无远端写入模拟；与 proM2 共用发布器和回归测试。

本地验收：

- 25 个目标单元测试通过，Python 编译通过；额外覆盖 exact head/base、trusted Settlement actor 与 merge SHA 门禁。
- airM2 模拟通过：17 files / 90,278 bytes / remote writes 0。
- GitHub 正式迁移、旧分支删除和最终 `0/0/0` 仍为 PENDING；按总任务约束在 37 个 Task 完成后统一执行。

回滚：仅 revert 本任务提交；在统一发布前不触碰远端分支。
