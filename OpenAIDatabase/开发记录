# OpenAIDatabase 开发记录

更新时间：2026-06-19

## 项目定位

OpenAIDatabase 是本地优先的 ChatGPT/Codex 记忆数据库，用于保存、蒸馏、检索和可视化用户的长期记忆、项目上下文、偏好、规则、历史决策和可复用 personalization。

## 当前已形成的开发状态

- `openai-memory-analysis` skill 可 ingest OpenAI export ZIP，生成 pending memory candidates、weekly/monthly packs、human-facing reports 和 SQLite search/fetch index。
- Memory Atlas 是独立 Vite + React + Three.js 本地可视化 app，不是第三方插件。
- Memory Atlas 支持数据源选择：`总数据源`、`ChatGPT`、`Codex`。
- 可视化板块包括 Galaxy、Notion Map、ROI、Obsidian Graph、Timeline、Contribution Grid、Word Cloud、Search/Review。
- 前端只读取 redacted derived visualization snapshot，不读取 raw exports 或直接修改 active memory。

## 最新 personalization 对本项目的要求

- 每次处理记忆后，聊天框必须输出人类可用结论：话题、要做什么、要记得什么、建议、ROI、成长、机会。
- 每轮要对比上一轮结论，说明新主题、增强/减弱主题、冲突、废弃、风险和机会。
- 每次处理要至少有战略/机会/ROI reviewer 和执行/质量 reviewer 两个视角。
- 记忆只分核心画像、一般、临时；不删除历史，不因降权而丢弃备份。
- 默认中文输出，专业术语和代码名可保留英文。

## 安全与执行边界

- 不提交 raw export ZIP、未脱敏 full messages、`.local_keys`、cookies、sessions、tokens、passwords、private keys、`.env`。
- 高风险 secrets 不以明文上传 GitHub；金融/交易 agent 通过 `secret_ref` 和本地 resolver 获取授权访问。
- Memory Atlas frontend 未来只能创建 versioned change proposals，不能直接改 `active_memory.jsonl`。

## 重要验证记录

```bash
python3 -m py_compile skills/openai-memory-analysis/scripts/openai_memory_analysis.py
python3 -m py_compile scripts/build_memory_atlas_data.py scripts/sync_codex_memory_data.py
python3 scripts/build_memory_atlas_data.py --database-dir . --output data/derived/visualization/memory_atlas.json
python3 -m unittest discover -s tests -p "test_*.py"
npm run build --prefix apps/memory-atlas
python3 scripts/audit_memory_atlas_visual_acceptance.py
python3 scripts/audit_memory_atlas_acceptance.py --publish-dir apps/memory-atlas/dist
```

## 下一步开发优先级

1. 继续强化 Memory Atlas 的真实交互、可视化信息密度和 human-facing 输出。
2. 完善前端 writeback proposal、版本控制和 rollback。
3. 扩展 Codex、WeChat、小红书、抖音等数据源的 canonical redacted derived events。
4. 保持本地运行、Cloudflare Pages + Access 和未来 Tauri 的边界清晰。
