# TSK.CodexProject.REPO1.0007 开发记录

## 目标与范围

- 目标：完成 `ST03/PHD` 的根入口、文档、注册表与清洁预算优化。
- Acceptance：`ACC.CodexProject.REPO1.0007`。
- Execution base：`79beaf0aa51b5d0e2914cead5fc4f3e660f8b430`；base tree `c3ddacd64d050d646ddcf315e3905b1ca1dab802`。
- 依赖：`.0006` local engineering PASS；本 Task 不升级 formal remote acceptance。
- 对象：根 AGENTS/README、root registry/generator、cleanliness policy/schema/auditor/test/CI、Task records 与 compact receipt。

## 上游开发序列

| Task | 本地证据 |
|---|---|
| `TSK.CodexProject.REPO1.0001` | read-only audit；无 tracked implementation commit |
| `TSK.CodexProject.REPO1.0002` | `cf4a26d54d8dc4f48fa943063ced2e3992237a92` |
| `TSK.CodexProject.REPO1.0003` | `a4085bcb9b0316037f46323f33b237c82fa89a2a` |
| `TSK.CodexProject.REPO1.0004` | `01dc6ff111dcc07cadf2c34139cab5baf85d14e4` |
| `TSK.CodexProject.REPO1.0005` | `773fd0f11c5d0cc65c45298455cb795904072628` |
| `TSK.OpenAIDatabase.CLEAN1.0001` | `57cb6bca623d2e01bde1a866140804c283340bd8` |
| `TSK.CodexProject.REPO1.0006` | `79beaf0aa51b5d0e2914cead5fc4f3e660f8b430` |

## 设计与实现

1. 以 `governance/projects.yaml` 作为迁移状态与 project identity 的唯一事实源；删除 AGENTS/README 的重复迁移表。
2. 删除 registry 中 `EEI` 与 `arxiv-daily-push` 的 root changed-scope exclusions：前者已 migrated，后者是 `ci_mode=required` active project，均不应绕过 root fan-out。
   移除后全项目 validator 暴露 arxiv roadmap 缺少其他 active project 已有的 `roadmap_kind: product`；本 Task 只补这一行兼容元数据，不改变 Stage、Task、状态、业务、runtime 或发送边界。
3. `root_cleanliness_budget.json` 显式登记 8 个根文件、10 个基础设施目录及动态 active/retired project 归属；完整文件不因 reference 不明而删除。
4. Auditor 只读 Git candidate tree，不访问网络、不跟随仓库外链接、不写 tracked/source；链接逃逸、duplicate、missing entry、budget overflow 与临时状态均 fail closed。
5. README 由 `generate_governance_dashboard.py::render_readme` 确定性生成，避免手工入口与 generator 再次漂移。

## 风险与控制

- 压缩丢失铁律：policy required tokens + byte budget + focused regression。
- Registry fan-out 扩大：仅恢复 required project 的预期 root-change coverage，由 changed-scope governance 验证。
- 错删/错移：本 Task 不删除或移动完整文件；重复 section 的 replacement 为 canonical registry。
- Prompt injection/credential：只解析受控 JSON/YAML/Markdown/Git path，不执行文档内容；新增行做 secret-prefix 扫描。
- Supply chain：无新增 dependency/action；CI 仅调用 stdlib auditor，并复用现有 YAML loader fallback。

## 验证状态

`PASS_LOCAL_ENGINEERING / REMOTE_PUBLICATION_DEFERRED`

| Gate | 结果 |
|---|---|
| Focused root cleanliness | `7/7 PASS` |
| 既有 root contract 选择性回归 | `7/7 PASS`，另有 331 项未选择 |
| Root audit | AGENTS `4084 bytes`；README `3170 bytes`；初始上下文 `7254 bytes`；root items `24/24 owned`；unowned/broken link/missing token/temporary pattern/unapproved exclusion 均 `0` |
| Active registry | active/retired/migrated `5/1/7`；README parity `5/5` |
| All-project governance | 5 个 active project；`0 errors / 0 warnings` |
| Changed-scope semantic/sync | `0 errors / 0 warnings` |
| Lean CI | `SHIP`；stable hash `bdf14517328f48ef0445dcecaac1e6c67021089d5b7cfbd39f635ef86c29624f`；zero tracked write |
| Artifact policy | `PASS`；canonical resource `6`；duplicate editable truth `0`；4 组 legacy digest exact |
| Governance ID V2 | `11,501 / 11,501` references exact；new positional ID `0` |
| Workflow security | `15/15 owned`；全部安全计数 `0`；render deterministic |
| Repository hygiene | retained large/archive `53/4`；runtime noise `0`；backup producer `0`；history rewrite `DEFERRED` |

可选 WDA retirement-history probe 为 `7/8`：唯一未运行成功的断言依赖隔离对象库中不存在的历史 commit `d26a9f2379b48fb5d01aba256f8f03d614c49305`。该 probe 不是本 Task Acceptance gate；按停止条件未为此执行大对象 fetch，也未改写 WDA。

## Stop Conditions 与 rollback

出现 credential、并发 writer、无关项目变更、必须提前远端写入、只能降低 hard gate 或 remote ancestry 冲突时停止。回滚为普通 revert 本 Task commit 后重跑 focused/root/changed-scope gates；禁止 history rewrite、force-push 或删除历史 evidence。

## 交付与运行

本 Task 只做 local commit；整包 37 Tasks 完成前 GitHub 写入保持 0。最终 publication Run 再做 remote ancestry/reconciliation、legacy branch disposition 与 terminal `0/0/0`。
