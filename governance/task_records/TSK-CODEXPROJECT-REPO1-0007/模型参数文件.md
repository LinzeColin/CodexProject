# TSK.CodexProject.REPO1.0007 模型参数文件

## 模型结论

`NOT_APPLICABLE`：本 Task 不新增数学、统计、ML、ranking、risk、业务公式、heuristic 或 LLM routing 模型。以下仅为确定性治理参数和集合公式。

## 参数

| 参数 | 值 | 原理/来源 | 失败行为 |
|---|---:|---|---|
| `agents_max_bytes` | 4096 | Task acceptance 与既有 Low-Token Contract | 非零退出 |
| `readme_max_bytes` | 8192 | 保证两入口合计仍可满足 12 KiB 上限 | 非零退出 |
| `initial_context_max_bytes` | 12288 | 既有普通初始治理上下文预算 | 非零退出 |
| `initial_context_max_files` | 2 | 根默认入口仅 AGENTS/README | 非零退出 |
| `unowned_root_item_max` | 0 | Task acceptance | 非零退出 |
| `broken_local_link_max` | 0 | Task acceptance | 非零退出 |
| `duplicate_registry_id_or_path_max` | 0 | Registry 唯一性 | 非零退出 |
| `unapproved_active_scope_exclusion_max` | 0 | required project 不得绕过 root change fan-out | 非零退出 |
| `migrated_path_present_max` | 0 | 防止恢复已迁出目录 | 非零退出 |

## 确定性公式

- `initial_context_bytes = bytes(AGENTS.md) + bytes(README.md)`。
- `actual_root_items = first_component(git ls-files --cached --others --exclude-standard)`。
- `owned_root_items = policy_static_items ∪ registry_active_paths ∪ registry_retired_paths`。
- `unowned_root_items = actual_root_items - owned_root_items`。
- `registry_identity_unique = unique(project_id) ∧ unique(path)`，范围覆盖 active/retired/migrated 三组。
- `PASS = policy_valid ∧ budgets_pass ∧ required_tokens_present ∧ temporary_state_count=0 ∧ unowned=0 ∧ broken_links=0 ∧ registry/README parity ∧ entrypoints_present`。

## 安全与可解释性

所有阈值直接记录于 JSON policy，并由 schema、测试与审计输出解释；没有概率阈值、训练数据、模型权重或隐藏默认值。Markdown link 仅验证仓库内路径，外部 URL 不发网络请求；任何指向仓库外的相对路径直接失败。
