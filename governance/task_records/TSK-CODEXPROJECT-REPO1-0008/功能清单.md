# TSK.CodexProject.REPO1.0008 功能清单

- Acceptance：`ACC.CodexProject.REPO1.0008`
- Placement：`ST04/PHA`
- Mode：`IMPLEMENT_AUTOMATION_C`
- Owner scope：`root-governance`

## 功能

1. Fresh-clone 独立验收：使用 `--depth 1 --no-local` 克隆当前本地候选 `main`，证明无 alternates、对象包独立、HEAD/tree 一致并通过 `git fsck --full --strict`。
2. Root CLEAN 硬门禁：重新计算 root ownership/context budget、Governance ID V2、workflow security、artifact boundary、tracked large/generated/cache/runtime noise 与 Automation C fixtures。
3. 远端终态事务：执行前后读取 GitHub PR、standalone Issue、Actions 与 branches；仅在 6/6 branch 具备 owner/replacement/hash/rollback 证明后删除 non-main refs。
4. 数据保全：EEI 以 MetaDatabase 导入提交中的精确 subtree OID 为 replacement；KMFA 以远端 `main` ancestry 为 replacement；两个 macdata tip 以本地待发布 `main` 的 34/34 精确 blob OID与临时 GitHub archive tags 双重保全。
5. Compact receipt：记录执行基线、命令/exit code、量化门禁、ref 映射、补偿方式、未解决风险与 self-excluding candidate tree hash。

## 操作入口

```bash
python3 -B scripts/root_cleanliness_audit.py --json
python3 -B scripts/workflow_security_audit.py audit --json
python3 -B scripts/governance_id_audit.py --json
python3 -B scripts/governance_artifact_policy.py audit --json
python3 -B scripts/repository_hygiene_audit.py --tree-ish HEAD
python3 -B -m unittest -q tests/governance/test_root_cleanliness.py tests/governance/test_workflow_security.py tests/governance/test_repository_hygiene.py tests/governance/test_governance_id_v2.py tests/governance/test_governance_artifact_policy.py
python3 -B -m unittest discover -s tests/agent_loop -p 'test_*.py' -q
```

## 验收硬门

- Root CLEAN Gate=`PASS`；root item `24/24 owned`；unowned/broken link/runtime noise/backup producer=`0`。
- duplicate/orphan Governance ID=`0`；fresh clone `11,501/11,501`、最终 staged candidate `11,503/11,503` references resolved。
- workflow security violations 全部为 `0`；`15/15` workflows owned。
- focused governance tests `38/38 PASS`；Automation C tests `21/21 PASS`。
- GitHub final `open PR=0 / standalone Issue=0 / non-main branch=0`；Actions in-progress/queued=`0/0`。

## 不包含

- 不修改项目业务、模型、公式、private/raw data、runtime、dependency 或 workflow。
- 不 push `main`、不创建 PR/Issue、不 force-push、不改写历史、不读取或执行 ST05+ Task。
- 两个临时 macdata archive tags 仅用于 whole-package publication 前远端对象保全；最终 `main` 内容核对通过后删除，不作为长期 canonical truth。
