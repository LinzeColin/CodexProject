# Updated Handoff Note

Sprint 2L is complete as a planning package.

Confirmed:

- Sprint 2L does not need the external hard drive.
- Sprint 2L does not run exporter tools.
- Sprint 2L does not create or modify `messages.jsonl`.
- Raw Gate remains `Bounded Multi-Message Proven`, not full Go.
- RAG/Web/Matrix remain blocked.

Recommended next step:

Sprint 2M-A should run on the old computer as a bounded first-batch subject
coverage export. It should use at most 5 subject targets, at most 100 messages
per subject/conversation, at most 500 total messages, and
`include_media_paths=false`.

Explicit exclusion:

`李晶工作交接` is a pollution/noise source and must not be selected as a subject
sample.

