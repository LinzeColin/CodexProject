# WDA Model Spec

## Status

`NOT_APPLICABLE` for the bootstrap phase.

WDA has no defined product scope yet, so there is no approved model, formula, scoring rule, LLM routing behavior, optimization logic, risk model, salary/business formula, or statistical/ML component.

## Guardrail

Future agents must not invent model behavior. If WDA later needs models, formulas, or parameters, define them first in:

- `docs/governance/model_registry.yaml`
- `docs/governance/formula_registry.yaml`
- `docs/governance/parameter_registry.csv`
- `模型参数文件.md`

## Evidence

- `EVID-WDA-BOOTSTRAP-001`: project shell created; product definition pending.
